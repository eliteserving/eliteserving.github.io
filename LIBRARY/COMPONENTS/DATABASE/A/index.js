export const ANCHOR=(HOLDER,SOURCE,CALLBACk)=>{
    ELEMENTED(HOLDER,"a",(DATA)=>{
        TEXTCOMPOMODE(DATA);
        WIDTH(DATA,"90%");
        SOURCEREF=(DATA,SOURCE);
        CALLBACk(DATA);
    });
};